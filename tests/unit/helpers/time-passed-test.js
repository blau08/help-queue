import { timePassed } from '../../../helpers/time-passed';
import { module, test } from 'qunit';

module('Unit | Helper | time passed');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = timePassed(42);
  assert.ok(result);
});
